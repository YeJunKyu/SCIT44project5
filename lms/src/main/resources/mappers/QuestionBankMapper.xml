<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scit.lms.dao.QuestionBankDAO">
    <!--모든 문제 조회-->
    <select id="selectAll" resultType="Question">
        select *
        from lms_question
        order by qid desc
    </select>
</mapper>
