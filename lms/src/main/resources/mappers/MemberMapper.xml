<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.scit.lms.dao.MemberDAO">
<insert id="join" parameterType="com.scit.lms.domain.Member">
    insert into lms_member(
        memberid
        , memberpw
        , membername
        , birthday
        , phone
        , gender
        , address
        , note
        , role
        , available)
    values(
        #{memberid}
        , #{memberpw}
        , #{membername}
        , #{birthday}
        , #{phone}
        , #{gender}
        , #{address}
        , #{note}
        , #{role}
        , #{available})
</insert>






</mapper>
